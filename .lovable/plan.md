

## Update GEMINI_API_KEY Secret

No code changes needed. I'll use the secrets tool to prompt you for the new key value. The edge functions (`underwriting-chat`, `underwriting-process`, `analyze-video`) will automatically use the new key on next invocation.

